{
  "name": "deeptravelcollection-minimal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p client/dist && cp -r client/public/* client/dist/",
    "start": "npx serve -s client/dist"
  }
}
